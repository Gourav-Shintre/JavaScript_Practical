// call back functions are passed as argument to another function  which has no name
const arr=[
    object={
        language:"java",
        extension:"java"
    },
    object2={
        language:"python",
        extension:"py"

    }
]
arr.forEach((i)=>{
    console.log(i.language,i.extension);

})